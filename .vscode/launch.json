{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run AG Command Dump",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}/.ag-tools/cmd-dump"
            ]
        }
    ]
}